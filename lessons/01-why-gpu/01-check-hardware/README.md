# Step 01 — Check hardware

What hardware does PyTorch see? No math yet — just look around.

**Run:** `python main.py`

**Expected:** the PyTorch version and whether a GPU is present.
`GPU available: False` on a laptop is normal — steps 03/04 fall back to
reference T4 numbers so you still see the comparison.
