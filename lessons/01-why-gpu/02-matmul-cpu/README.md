# Step 02 — Matmul on CPU

Time one 2000×2000 matrix multiply on the CPU. This is the baseline
before we try the GPU.

**Run:** `python main.py`

**Expected:** `Time: ~150 ms` on a typical laptop (varies by machine).
